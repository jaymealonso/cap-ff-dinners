{
  "name": "cap-ff-dinner",
  "version": "1.0.0",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "@sap/xssec": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve",
    "debug": "cds watch --inspect"
  },
  "private": true,
  "cds": {
    "fiori": {
      "preview": true
    },
    "server": {
      "index": true
    },
    "requires": {
      "auth": "mocked",
      "[production]": {
        "auth": "xsuaa",
        "db": "hana"
      }
    }
  }
}