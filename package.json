{
  "name": "cap-ff-dinner",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^9"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2"
  },
  "scripts": {
    "start": "cds-serve",
    "debug": "cds watch --inspect"
  },
  "private": true,
  "cds": {
    "requires": {
      "auth": "mocked"
    }
  }
}